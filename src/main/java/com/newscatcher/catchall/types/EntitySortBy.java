/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class EntitySortBy {
    public static final EntitySortBy STATUS = new EntitySortBy(Value.STATUS, "status");

    public static final EntitySortBy CREATED_AT = new EntitySortBy(Value.CREATED_AT, "created_at");

    public static final EntitySortBy NAME = new EntitySortBy(Value.NAME, "name");

    private final Value value;

    private final String string;

    EntitySortBy(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof EntitySortBy && this.string.equals(((EntitySortBy) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case STATUS:
                return visitor.visitStatus();
            case CREATED_AT:
                return visitor.visitCreatedAt();
            case NAME:
                return visitor.visitName();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static EntitySortBy valueOf(String value) {
        switch (value) {
            case "status":
                return STATUS;
            case "created_at":
                return CREATED_AT;
            case "name":
                return NAME;
            default:
                return new EntitySortBy(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        CREATED_AT,

        NAME,

        STATUS,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCreatedAt();

        T visitName();

        T visitStatus();

        T visitUnknown(String unknownType);
    }
}
