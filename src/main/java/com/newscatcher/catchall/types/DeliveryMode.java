/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DeliveryMode {
    public static final DeliveryMode FULL = new DeliveryMode(Value.FULL, "full");

    public static final DeliveryMode PER_RECORD = new DeliveryMode(Value.PER_RECORD, "per_record");

    private final Value value;

    private final String string;

    DeliveryMode(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof DeliveryMode && this.string.equals(((DeliveryMode) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FULL:
                return visitor.visitFull();
            case PER_RECORD:
                return visitor.visitPerRecord();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DeliveryMode valueOf(String value) {
        switch (value) {
            case "full":
                return FULL;
            case "per_record":
                return PER_RECORD;
            default:
                return new DeliveryMode(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        FULL,

        PER_RECORD,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitFull();

        T visitPerRecord();

        T visitUnknown(String unknownType);
    }
}
