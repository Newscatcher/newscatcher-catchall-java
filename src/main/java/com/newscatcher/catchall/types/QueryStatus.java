/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class QueryStatus {
    public static final QueryStatus NEEDS_WORK = new QueryStatus(Value.NEEDS_WORK, "needs_work");

    public static final QueryStatus CRITICAL = new QueryStatus(Value.CRITICAL, "critical");

    public static final QueryStatus GOOD = new QueryStatus(Value.GOOD, "good");

    private final Value value;

    private final String string;

    QueryStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof QueryStatus && this.string.equals(((QueryStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case NEEDS_WORK:
                return visitor.visitNeedsWork();
            case CRITICAL:
                return visitor.visitCritical();
            case GOOD:
                return visitor.visitGood();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static QueryStatus valueOf(String value) {
        switch (value) {
            case "needs_work":
                return NEEDS_WORK;
            case "critical":
                return CRITICAL;
            case "good":
                return GOOD;
            default:
                return new QueryStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        GOOD,

        NEEDS_WORK,

        CRITICAL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitGood();

        T visitNeedsWork();

        T visitCritical();

        T visitUnknown(String unknownType);
    }
}
