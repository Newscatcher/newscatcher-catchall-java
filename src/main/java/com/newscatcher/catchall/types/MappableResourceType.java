/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class MappableResourceType {
    public static final MappableResourceType MONITOR = new MappableResourceType(Value.MONITOR, "monitor");

    public static final MappableResourceType JOB = new MappableResourceType(Value.JOB, "job");

    public static final MappableResourceType MONITOR_GROUP =
            new MappableResourceType(Value.MONITOR_GROUP, "monitor_group");

    private final Value value;

    private final String string;

    MappableResourceType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof MappableResourceType && this.string.equals(((MappableResourceType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case MONITOR:
                return visitor.visitMonitor();
            case JOB:
                return visitor.visitJob();
            case MONITOR_GROUP:
                return visitor.visitMonitorGroup();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static MappableResourceType valueOf(String value) {
        switch (value) {
            case "monitor":
                return MONITOR;
            case "job":
                return JOB;
            case "monitor_group":
                return MONITOR_GROUP;
            default:
                return new MappableResourceType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        JOB,

        MONITOR,

        MONITOR_GROUP,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitJob();

        T visitMonitor();

        T visitMonitorGroup();

        T visitUnknown(String unknownType);
    }
}
