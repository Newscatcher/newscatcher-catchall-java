/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.resources.monitors;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.newscatcher.catchall.core.CatchAllApiApiException;
import com.newscatcher.catchall.core.CatchAllApiException;
import com.newscatcher.catchall.core.CatchAllApiHttpResponse;
import com.newscatcher.catchall.core.ClientOptions;
import com.newscatcher.catchall.core.MediaTypes;
import com.newscatcher.catchall.core.ObjectMappers;
import com.newscatcher.catchall.core.QueryStringMapper;
import com.newscatcher.catchall.core.RequestOptions;
import com.newscatcher.catchall.errors.ForbiddenError;
import com.newscatcher.catchall.errors.NotFoundError;
import com.newscatcher.catchall.errors.UnprocessableEntityError;
import com.newscatcher.catchall.resources.monitors.requests.CreateMonitorRequestDto;
import com.newscatcher.catchall.resources.monitors.requests.DisableMonitorRequest;
import com.newscatcher.catchall.resources.monitors.requests.EnableMonitorRequest;
import com.newscatcher.catchall.resources.monitors.requests.ListMonitorJobsRequest;
import com.newscatcher.catchall.resources.monitors.requests.PullMonitorResultsRequest;
import com.newscatcher.catchall.resources.monitors.types.DisableMonitorResponse;
import com.newscatcher.catchall.resources.monitors.types.EnableMonitorResponse;
import com.newscatcher.catchall.resources.monitors.types.ListMonitorJobsResponse;
import com.newscatcher.catchall.types.CreateMonitorResponseDto;
import com.newscatcher.catchall.types.Error;
import com.newscatcher.catchall.types.ListMonitorsResponseDto;
import com.newscatcher.catchall.types.PullMonitorResponseDto;
import com.newscatcher.catchall.types.ValidationErrorResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawMonitorsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawMonitorsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Create a monitor that runs jobs based on a reference job with a specified schedule.
     * <p><strong>Schedule requirements:</strong></p>
     * <ul>
     * <li>Minimum 24-hour interval between executions</li>
     * <li>Natural language format (e.g., &quot;every day at 12 PM UTC&quot;, &quot;every 48 hours&quot;)</li>
     * </ul>
     * <p><strong>Validation:</strong></p>
     * <ul>
     * <li>Schedules below minimum frequency return error with descriptive message.</li>
     * <li>Invalid job IDs return 400 Bad Request.</li>
     * <li>Duplicate monitors (same job already monitored) return error.</li>
     * </ul>
     */
    public CompletableFuture<CatchAllApiHttpResponse<CreateMonitorResponseDto>> createMonitor(
            CreateMonitorRequestDto request) {
        return createMonitor(request, null);
    }

    /**
     * Create a monitor that runs jobs based on a reference job with a specified schedule.
     * <p><strong>Schedule requirements:</strong></p>
     * <ul>
     * <li>Minimum 24-hour interval between executions</li>
     * <li>Natural language format (e.g., &quot;every day at 12 PM UTC&quot;, &quot;every 48 hours&quot;)</li>
     * </ul>
     * <p><strong>Validation:</strong></p>
     * <ul>
     * <li>Schedules below minimum frequency return error with descriptive message.</li>
     * <li>Invalid job IDs return 400 Bad Request.</li>
     * <li>Duplicate monitors (same job already monitored) return error.</li>
     * </ul>
     */
    public CompletableFuture<CatchAllApiHttpResponse<CreateMonitorResponseDto>> createMonitor(
            CreateMonitorRequestDto request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors/create")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new CatchAllApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<CreateMonitorResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateMonitorResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(
                                            responseBodyString, ValidationErrorResponse.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns all jobs associated with a monitor, sorted by start_date.
     * Each job includes job_id, start_date, and end_date.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListMonitorJobsResponse>> listMonitorJobs(String monitorId) {
        return listMonitorJobs(monitorId, ListMonitorJobsRequest.builder().build());
    }

    /**
     * Returns all jobs associated with a monitor, sorted by start_date.
     * Each job includes job_id, start_date, and end_date.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListMonitorJobsResponse>> listMonitorJobs(
            String monitorId, ListMonitorJobsRequest request) {
        return listMonitorJobs(monitorId, request, null);
    }

    /**
     * Returns all jobs associated with a monitor, sorted by start_date.
     * Each job includes job_id, start_date, and end_date.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListMonitorJobsResponse>> listMonitorJobs(
            String monitorId, ListMonitorJobsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors")
                .addPathSegment(monitorId)
                .addPathSegments("jobs");
        if (request.getSort().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort", request.getSort().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<ListMonitorJobsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListMonitorJobsResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieve aggregated results from all jobs executed by this monitor.
     * Includes monitor configuration, execution history, and all records collected.
     */
    public CompletableFuture<CatchAllApiHttpResponse<PullMonitorResponseDto>> pullMonitorResults(String monitorId) {
        return pullMonitorResults(monitorId, PullMonitorResultsRequest.builder().build());
    }

    /**
     * Retrieve aggregated results from all jobs executed by this monitor.
     * Includes monitor configuration, execution history, and all records collected.
     */
    public CompletableFuture<CatchAllApiHttpResponse<PullMonitorResponseDto>> pullMonitorResults(
            String monitorId, PullMonitorResultsRequest request) {
        return pullMonitorResults(monitorId, request, null);
    }

    /**
     * Retrieve aggregated results from all jobs executed by this monitor.
     * Includes monitor configuration, execution history, and all records collected.
     */
    public CompletableFuture<CatchAllApiHttpResponse<PullMonitorResponseDto>> pullMonitorResults(
            String monitorId, PullMonitorResultsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors/pull")
                .addPathSegment(monitorId)
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<PullMonitorResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PullMonitorResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Disables a monitor to stop executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<DisableMonitorResponse>> disableMonitor(String monitorId) {
        return disableMonitor(monitorId, DisableMonitorRequest.builder().build());
    }

    /**
     * Disables a monitor to stop executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<DisableMonitorResponse>> disableMonitor(
            String monitorId, DisableMonitorRequest request) {
        return disableMonitor(monitorId, request, null);
    }

    /**
     * Disables a monitor to stop executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<DisableMonitorResponse>> disableMonitor(
            String monitorId, DisableMonitorRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors")
                .addPathSegment(monitorId)
                .addPathSegments("disable")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<DisableMonitorResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DisableMonitorResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Enables a monitor to resume executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<EnableMonitorResponse>> enableMonitor(String monitorId) {
        return enableMonitor(monitorId, EnableMonitorRequest.builder().build());
    }

    /**
     * Enables a monitor to resume executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<EnableMonitorResponse>> enableMonitor(
            String monitorId, EnableMonitorRequest request) {
        return enableMonitor(monitorId, request, null);
    }

    /**
     * Enables a monitor to resume executing scheduled jobs.
     * Validates that the provided API key is associated with the monitor.
     */
    public CompletableFuture<CatchAllApiHttpResponse<EnableMonitorResponse>> enableMonitor(
            String monitorId, EnableMonitorRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors")
                .addPathSegment(monitorId)
                .addPathSegments("enable")
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<EnableMonitorResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EnableMonitorResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns all monitors created by the authenticated user.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListMonitorsResponseDto>> listMonitors() {
        return listMonitors(null);
    }

    /**
     * Returns all monitors created by the authenticated user.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListMonitorsResponseDto>> listMonitors(
            RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/monitors")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<ListMonitorsResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListMonitorsResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
