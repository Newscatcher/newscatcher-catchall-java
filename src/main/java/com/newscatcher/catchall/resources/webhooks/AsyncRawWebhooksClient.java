/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.resources.webhooks;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.newscatcher.catchall.core.CatchAllApiApiException;
import com.newscatcher.catchall.core.CatchAllApiException;
import com.newscatcher.catchall.core.CatchAllApiHttpResponse;
import com.newscatcher.catchall.core.ClientOptions;
import com.newscatcher.catchall.core.MediaTypes;
import com.newscatcher.catchall.core.ObjectMappers;
import com.newscatcher.catchall.core.QueryStringMapper;
import com.newscatcher.catchall.core.RequestOptions;
import com.newscatcher.catchall.errors.BadRequestError;
import com.newscatcher.catchall.errors.ForbiddenError;
import com.newscatcher.catchall.errors.NotFoundError;
import com.newscatcher.catchall.errors.UnprocessableEntityError;
import com.newscatcher.catchall.resources.webhooks.requests.AssignWebhookResourceRequestDto;
import com.newscatcher.catchall.resources.webhooks.requests.CreateWebhookRequestDto;
import com.newscatcher.catchall.resources.webhooks.requests.DeleteWebhookRequest;
import com.newscatcher.catchall.resources.webhooks.requests.GetWebhookDeliveryHistoryRequest;
import com.newscatcher.catchall.resources.webhooks.requests.GetWebhookRequest;
import com.newscatcher.catchall.resources.webhooks.requests.ListWebhookResourcesRequest;
import com.newscatcher.catchall.resources.webhooks.requests.ListWebhooksForResourceRequest;
import com.newscatcher.catchall.resources.webhooks.requests.ListWebhooksRequest;
import com.newscatcher.catchall.resources.webhooks.requests.RemoveWebhookResourceRequest;
import com.newscatcher.catchall.resources.webhooks.requests.TestWebhookRequestDto;
import com.newscatcher.catchall.resources.webhooks.requests.UpdateWebhookRequestDto;
import com.newscatcher.catchall.types.AssignWebhookResourceResponseDto;
import com.newscatcher.catchall.types.CreateWebhookResponseDto;
import com.newscatcher.catchall.types.DeliveryHistoryResponseDto;
import com.newscatcher.catchall.types.Error;
import com.newscatcher.catchall.types.GetWebhookResponseDto;
import com.newscatcher.catchall.types.ListWebhookResourcesResponseDto;
import com.newscatcher.catchall.types.ListWebhooksResponseDto;
import com.newscatcher.catchall.types.MappableResourceType;
import com.newscatcher.catchall.types.TestWebhookResponseDto;
import com.newscatcher.catchall.types.UpdateWebhookResponseDto;
import com.newscatcher.catchall.types.ValidationErrorResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawWebhooksClient {
    protected final ClientOptions clientOptions;

    public AsyncRawWebhooksClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of webhooks belonging to the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooks() {
        return listWebhooks(ListWebhooksRequest.builder().build());
    }

    /**
     * Returns a paginated list of webhooks belonging to the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooks(
            RequestOptions requestOptions) {
        return listWebhooks(ListWebhooksRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of webhooks belonging to the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooks(
            ListWebhooksRequest request) {
        return listWebhooks(request, null);
    }

    /**
     * Returns a paginated list of webhooks belonging to the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooks(
            ListWebhooksRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_size", request.getPageSize().get(), false);
        }
        if (request.getSearch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "search", request.getSearch().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListWebhooksResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Creates a new webhook endpoint for the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<CreateWebhookResponseDto>> createWebhook(
            CreateWebhookRequestDto request) {
        return createWebhook(request, null);
    }

    /**
     * Creates a new webhook endpoint for the organization.
     */
    public CompletableFuture<CatchAllApiHttpResponse<CreateWebhookResponseDto>> createWebhook(
            CreateWebhookRequestDto request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new CatchAllApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<CreateWebhookResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateWebhookResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns the full configuration of a single webhook by ID.
     */
    public CompletableFuture<CatchAllApiHttpResponse<GetWebhookResponseDto>> getWebhook(String webhookId) {
        return getWebhook(webhookId, GetWebhookRequest.builder().build());
    }

    /**
     * Returns the full configuration of a single webhook by ID.
     */
    public CompletableFuture<CatchAllApiHttpResponse<GetWebhookResponseDto>> getWebhook(
            String webhookId, RequestOptions requestOptions) {
        return getWebhook(webhookId, GetWebhookRequest.builder().build(), requestOptions);
    }

    /**
     * Returns the full configuration of a single webhook by ID.
     */
    public CompletableFuture<CatchAllApiHttpResponse<GetWebhookResponseDto>> getWebhook(
            String webhookId, GetWebhookRequest request) {
        return getWebhook(webhookId, request, null);
    }

    /**
     * Returns the full configuration of a single webhook by ID.
     */
    public CompletableFuture<CatchAllApiHttpResponse<GetWebhookResponseDto>> getWebhook(
            String webhookId, GetWebhookRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<GetWebhookResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetWebhookResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Permanently deletes a webhook and removes all resource assignments.
     * <p>Assigned jobs and monitors no longer trigger delivery to this webhook. This operation cannot be undone.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> deleteWebhook(String webhookId) {
        return deleteWebhook(webhookId, DeleteWebhookRequest.builder().build());
    }

    /**
     * Permanently deletes a webhook and removes all resource assignments.
     * <p>Assigned jobs and monitors no longer trigger delivery to this webhook. This operation cannot be undone.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> deleteWebhook(
            String webhookId, RequestOptions requestOptions) {
        return deleteWebhook(webhookId, DeleteWebhookRequest.builder().build(), requestOptions);
    }

    /**
     * Permanently deletes a webhook and removes all resource assignments.
     * <p>Assigned jobs and monitors no longer trigger delivery to this webhook. This operation cannot be undone.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> deleteWebhook(
            String webhookId, DeleteWebhookRequest request) {
        return deleteWebhook(webhookId, request, null);
    }

    /**
     * Permanently deletes a webhook and removes all resource assignments.
     * <p>Assigned jobs and monitors no longer trigger delivery to this webhook. This operation cannot be undone.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> deleteWebhook(
            String webhookId, DeleteWebhookRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Updates one or more fields of an existing webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<UpdateWebhookResponseDto>> updateWebhook(String webhookId) {
        return updateWebhook(webhookId, UpdateWebhookRequestDto.builder().build());
    }

    /**
     * Updates one or more fields of an existing webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<UpdateWebhookResponseDto>> updateWebhook(
            String webhookId, RequestOptions requestOptions) {
        return updateWebhook(webhookId, UpdateWebhookRequestDto.builder().build(), requestOptions);
    }

    /**
     * Updates one or more fields of an existing webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<UpdateWebhookResponseDto>> updateWebhook(
            String webhookId, UpdateWebhookRequestDto request) {
        return updateWebhook(webhookId, request, null);
    }

    /**
     * Updates one or more fields of an existing webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<UpdateWebhookResponseDto>> updateWebhook(
            String webhookId, UpdateWebhookRequestDto request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new CatchAllApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<UpdateWebhookResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateWebhookResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Sends a test HTTP request to the webhook URL using the webhook's configured method, headers, and auth. Returns the response from the target endpoint.
     * <p>Use this to verify URL reachability and authentication before attaching the webhook to a live job or monitor.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<TestWebhookResponseDto>> testWebhook(String webhookId) {
        return testWebhook(webhookId, TestWebhookRequestDto.builder().build());
    }

    /**
     * Sends a test HTTP request to the webhook URL using the webhook's configured method, headers, and auth. Returns the response from the target endpoint.
     * <p>Use this to verify URL reachability and authentication before attaching the webhook to a live job or monitor.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<TestWebhookResponseDto>> testWebhook(
            String webhookId, RequestOptions requestOptions) {
        return testWebhook(webhookId, TestWebhookRequestDto.builder().build(), requestOptions);
    }

    /**
     * Sends a test HTTP request to the webhook URL using the webhook's configured method, headers, and auth. Returns the response from the target endpoint.
     * <p>Use this to verify URL reachability and authentication before attaching the webhook to a live job or monitor.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<TestWebhookResponseDto>> testWebhook(
            String webhookId, TestWebhookRequestDto request) {
        return testWebhook(webhookId, request, null);
    }

    /**
     * Sends a test HTTP request to the webhook URL using the webhook's configured method, headers, and auth. Returns the response from the target endpoint.
     * <p>Use this to verify URL reachability and authentication before attaching the webhook to a live job or monitor.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<TestWebhookResponseDto>> testWebhook(
            String webhookId, TestWebhookRequestDto request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId)
                .addPathSegments("test");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new CatchAllApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<TestWebhookResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TestWebhookResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a paginated list of resources currently assigned to this webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhookResourcesResponseDto>> listWebhookResources(
            String webhookId) {
        return listWebhookResources(
                webhookId, ListWebhookResourcesRequest.builder().build());
    }

    /**
     * Returns a paginated list of resources currently assigned to this webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhookResourcesResponseDto>> listWebhookResources(
            String webhookId, RequestOptions requestOptions) {
        return listWebhookResources(
                webhookId, ListWebhookResourcesRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of resources currently assigned to this webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhookResourcesResponseDto>> listWebhookResources(
            String webhookId, ListWebhookResourcesRequest request) {
        return listWebhookResources(webhookId, request, null);
    }

    /**
     * Returns a paginated list of resources currently assigned to this webhook.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhookResourcesResponseDto>> listWebhookResources(
            String webhookId, ListWebhookResourcesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId)
                .addPathSegments("resources");
        if (request.getResourceType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "resource_type", request.getResourceType().get(), false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_size", request.getPageSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<ListWebhookResourcesResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ListWebhookResourcesResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Attaches a job, monitor, or monitor group to the webhook. When the
     * resource completes, the webhook receives a delivery.
     * <p>A single webhook can be assigned to multiple resources. Each resource
     * can have up to 5 webhooks assigned.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<AssignWebhookResourceResponseDto>> assignWebhookResource(
            String webhookId, AssignWebhookResourceRequestDto request) {
        return assignWebhookResource(webhookId, request, null);
    }

    /**
     * Attaches a job, monitor, or monitor group to the webhook. When the
     * resource completes, the webhook receives a delivery.
     * <p>A single webhook can be assigned to multiple resources. Each resource
     * can have up to 5 webhooks assigned.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<AssignWebhookResourceResponseDto>> assignWebhookResource(
            String webhookId, AssignWebhookResourceRequestDto request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId)
                .addPathSegments("resources");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new CatchAllApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<AssignWebhookResourceResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, AssignWebhookResourceResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Detaches a resource from this webhook. Completions of the resource no longer trigger delivery to this webhook.
     * <p>The webhook and the resource itself are not deleted.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> removeWebhookResource(
            String webhookId, MappableResourceType resourceType, String resourceId) {
        return removeWebhookResource(
                webhookId,
                resourceType,
                resourceId,
                RemoveWebhookResourceRequest.builder().build());
    }

    /**
     * Detaches a resource from this webhook. Completions of the resource no longer trigger delivery to this webhook.
     * <p>The webhook and the resource itself are not deleted.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> removeWebhookResource(
            String webhookId, MappableResourceType resourceType, String resourceId, RequestOptions requestOptions) {
        return removeWebhookResource(
                webhookId,
                resourceType,
                resourceId,
                RemoveWebhookResourceRequest.builder().build(),
                requestOptions);
    }

    /**
     * Detaches a resource from this webhook. Completions of the resource no longer trigger delivery to this webhook.
     * <p>The webhook and the resource itself are not deleted.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> removeWebhookResource(
            String webhookId,
            MappableResourceType resourceType,
            String resourceId,
            RemoveWebhookResourceRequest request) {
        return removeWebhookResource(webhookId, resourceType, resourceId, request, null);
    }

    /**
     * Detaches a resource from this webhook. Completions of the resource no longer trigger delivery to this webhook.
     * <p>The webhook and the resource itself are not deleted.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<Void>> removeWebhookResource(
            String webhookId,
            MappableResourceType resourceType,
            String resourceId,
            RemoveWebhookResourceRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhooks")
                .addPathSegment(webhookId)
                .addPathSegments("resources")
                .addPathSegment(resourceType.toString())
                .addPathSegment(resourceId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns all webhooks currently assigned to the given resource.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooksForResource(
            MappableResourceType resourceType, String resourceId) {
        return listWebhooksForResource(
                resourceType,
                resourceId,
                ListWebhooksForResourceRequest.builder().build());
    }

    /**
     * Returns all webhooks currently assigned to the given resource.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooksForResource(
            MappableResourceType resourceType, String resourceId, RequestOptions requestOptions) {
        return listWebhooksForResource(
                resourceType,
                resourceId,
                ListWebhooksForResourceRequest.builder().build(),
                requestOptions);
    }

    /**
     * Returns all webhooks currently assigned to the given resource.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooksForResource(
            MappableResourceType resourceType, String resourceId, ListWebhooksForResourceRequest request) {
        return listWebhooksForResource(resourceType, resourceId, request, null);
    }

    /**
     * Returns all webhooks currently assigned to the given resource.
     */
    public CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> listWebhooksForResource(
            MappableResourceType resourceType,
            String resourceId,
            ListWebhooksForResourceRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/resources")
                .addPathSegment(resourceType.toString())
                .addPathSegment(resourceId)
                .addPathSegments("webhooks");
        if (request.getIsActive().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "is_active", request.getIsActive().get(), false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_size", request.getPageSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<ListWebhooksResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListWebhooksResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a paginated delivery log for a given resource, ordered by timestamp descending.
     * <p>Each record shows the webhook dispatched, the HTTP status code returned, delivery outcome, and any error or warning messages. Use this to debug failed deliveries or audit dispatch activity.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<DeliveryHistoryResponseDto>> getWebhookDeliveryHistory(
            GetWebhookDeliveryHistoryRequest request) {
        return getWebhookDeliveryHistory(request, null);
    }

    /**
     * Returns a paginated delivery log for a given resource, ordered by timestamp descending.
     * <p>Each record shows the webhook dispatched, the HTTP status code returned, delivery outcome, and any error or warning messages. Use this to debug failed deliveries or audit dispatch activity.</p>
     */
    public CompletableFuture<CatchAllApiHttpResponse<DeliveryHistoryResponseDto>> getWebhookDeliveryHistory(
            GetWebhookDeliveryHistoryRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("catchAll/webhook-history");
        QueryStringMapper.addQueryParameter(httpUrl, "resource_type", request.getResourceType(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "resource_id", request.getResourceId(), false);
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_size", request.getPageSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CatchAllApiHttpResponse<DeliveryHistoryResponseDto>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new CatchAllApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, DeliveryHistoryResponseDto.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, ValidationErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CatchAllApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CatchAllApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
