/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.resources.webhooks.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.newscatcher.catchall.core.Nullable;
import com.newscatcher.catchall.core.NullableNonemptyFilter;
import com.newscatcher.catchall.core.ObjectMappers;
import com.newscatcher.catchall.resources.webhooks.types.CreateWebhookRequestDtoAuth;
import com.newscatcher.catchall.types.DeliveryMode;
import com.newscatcher.catchall.types.FormatterConfigDto;
import com.newscatcher.catchall.types.HttpMethod;
import com.newscatcher.catchall.types.WebhookType;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateWebhookRequestDto.Builder.class)
public final class CreateWebhookRequestDto {
    private final String name;

    private final String url;

    private final Optional<WebhookType> type;

    private final Optional<DeliveryMode> deliveryMode;

    private final Optional<HttpMethod> method;

    private final Optional<Map<String, String>> headers;

    private final Optional<Map<String, String>> params;

    private final Optional<CreateWebhookRequestDtoAuth> auth;

    private final Optional<FormatterConfigDto> formatterConfig;

    private final Map<String, Object> additionalProperties;

    private CreateWebhookRequestDto(
            String name,
            String url,
            Optional<WebhookType> type,
            Optional<DeliveryMode> deliveryMode,
            Optional<HttpMethod> method,
            Optional<Map<String, String>> headers,
            Optional<Map<String, String>> params,
            Optional<CreateWebhookRequestDtoAuth> auth,
            Optional<FormatterConfigDto> formatterConfig,
            Map<String, Object> additionalProperties) {
        this.name = name;
        this.url = url;
        this.type = type;
        this.deliveryMode = deliveryMode;
        this.method = method;
        this.headers = headers;
        this.params = params;
        this.auth = auth;
        this.formatterConfig = formatterConfig;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Human-readable label for this webhook.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return Destination URL that receives the payload. Must use HTTPS. IP addresses are not accepted.
     * <p>Type-specific URL requirements:</p>
     * <ul>
     * <li><code>slack</code>: Must start with <code>https://hooks.slack.com/</code>.</li>
     * <li><code>teams</code>: Hostname must match <code>*.webhook.office.com</code> or <code>*.webhook.office365.com</code>.</li>
     * <li><code>generic</code>: Any valid HTTPS domain.</li>
     * <li><code>custom</code>: Any valid HTTPS domain.</li>
     * </ul>
     * <p>When <code>type</code> is omitted, it is auto-detected from the URL.</p>
     */
    @JsonProperty("url")
    public String getUrl() {
        return url;
    }

    @JsonProperty("type")
    public Optional<WebhookType> getType() {
        return type;
    }

    @JsonProperty("delivery_mode")
    public Optional<DeliveryMode> getDeliveryMode() {
        return deliveryMode;
    }

    @JsonProperty("method")
    public Optional<HttpMethod> getMethod() {
        return method;
    }

    /**
     * @return Custom HTTP headers forwarded with each delivery.
     */
    @JsonProperty("headers")
    public Optional<Map<String, String>> getHeaders() {
        return headers;
    }

    /**
     * @return Query parameters appended to the webhook URL.
     */
    @JsonProperty("params")
    public Optional<Map<String, String>> getParams() {
        return params;
    }

    /**
     * @return Authentication forwarded with each delivery. Supported types:
     * <ul>
     * <li><code>bearer</code>: Adds an <code>Authorization: Bearer &lt;token&gt;</code> header.</li>
     * <li><code>api_key</code>: Adds a custom header with the specified name and value.</li>
     * <li><code>basic</code>: Adds an <code>Authorization: Basic &lt;credentials&gt;</code> header.</li>
     * </ul>
     */
    @JsonProperty("auth")
    public Optional<CreateWebhookRequestDtoAuth> getAuth() {
        return auth;
    }

    /**
     * @return Custom payload formatter. Required when <code>type</code> is <code>custom</code>.
     */
    @JsonIgnore
    public Optional<FormatterConfigDto> getFormatterConfig() {
        if (formatterConfig == null) {
            return Optional.empty();
        }
        return formatterConfig;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("formatter_config")
    private Optional<FormatterConfigDto> _getFormatterConfig() {
        return formatterConfig;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateWebhookRequestDto && equalTo((CreateWebhookRequestDto) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateWebhookRequestDto other) {
        return name.equals(other.name)
                && url.equals(other.url)
                && type.equals(other.type)
                && deliveryMode.equals(other.deliveryMode)
                && method.equals(other.method)
                && headers.equals(other.headers)
                && params.equals(other.params)
                && auth.equals(other.auth)
                && formatterConfig.equals(other.formatterConfig);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.name,
                this.url,
                this.type,
                this.deliveryMode,
                this.method,
                this.headers,
                this.params,
                this.auth,
                this.formatterConfig);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static NameStage builder() {
        return new Builder();
    }

    public interface NameStage {
        /**
         * <p>Human-readable label for this webhook.</p>
         */
        UrlStage name(@NotNull String name);

        Builder from(CreateWebhookRequestDto other);
    }

    public interface UrlStage {
        /**
         * <p>Destination URL that receives the payload. Must use HTTPS. IP addresses are not accepted.</p>
         * <p>Type-specific URL requirements:</p>
         * <ul>
         * <li><code>slack</code>: Must start with <code>https://hooks.slack.com/</code>.</li>
         * <li><code>teams</code>: Hostname must match <code>*.webhook.office.com</code> or <code>*.webhook.office365.com</code>.</li>
         * <li><code>generic</code>: Any valid HTTPS domain.</li>
         * <li><code>custom</code>: Any valid HTTPS domain.</li>
         * </ul>
         * <p>When <code>type</code> is omitted, it is auto-detected from the URL.</p>
         */
        _FinalStage url(@NotNull String url);
    }

    public interface _FinalStage {
        CreateWebhookRequestDto build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        _FinalStage type(Optional<WebhookType> type);

        _FinalStage type(WebhookType type);

        _FinalStage deliveryMode(Optional<DeliveryMode> deliveryMode);

        _FinalStage deliveryMode(DeliveryMode deliveryMode);

        _FinalStage method(Optional<HttpMethod> method);

        _FinalStage method(HttpMethod method);

        /**
         * <p>Custom HTTP headers forwarded with each delivery.</p>
         */
        _FinalStage headers(Optional<Map<String, String>> headers);

        _FinalStage headers(Map<String, String> headers);

        /**
         * <p>Query parameters appended to the webhook URL.</p>
         */
        _FinalStage params(Optional<Map<String, String>> params);

        _FinalStage params(Map<String, String> params);

        /**
         * <p>Authentication forwarded with each delivery. Supported types:</p>
         * <ul>
         * <li><code>bearer</code>: Adds an <code>Authorization: Bearer &lt;token&gt;</code> header.</li>
         * <li><code>api_key</code>: Adds a custom header with the specified name and value.</li>
         * <li><code>basic</code>: Adds an <code>Authorization: Basic &lt;credentials&gt;</code> header.</li>
         * </ul>
         */
        _FinalStage auth(Optional<CreateWebhookRequestDtoAuth> auth);

        _FinalStage auth(CreateWebhookRequestDtoAuth auth);

        /**
         * <p>Custom payload formatter. Required when <code>type</code> is <code>custom</code>.</p>
         */
        _FinalStage formatterConfig(Optional<FormatterConfigDto> formatterConfig);

        _FinalStage formatterConfig(FormatterConfigDto formatterConfig);

        _FinalStage formatterConfig(Nullable<FormatterConfigDto> formatterConfig);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements NameStage, UrlStage, _FinalStage {
        private String name;

        private String url;

        private Optional<FormatterConfigDto> formatterConfig = Optional.empty();

        private Optional<CreateWebhookRequestDtoAuth> auth = Optional.empty();

        private Optional<Map<String, String>> params = Optional.empty();

        private Optional<Map<String, String>> headers = Optional.empty();

        private Optional<HttpMethod> method = Optional.empty();

        private Optional<DeliveryMode> deliveryMode = Optional.empty();

        private Optional<WebhookType> type = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateWebhookRequestDto other) {
            name(other.getName());
            url(other.getUrl());
            type(other.getType());
            deliveryMode(other.getDeliveryMode());
            method(other.getMethod());
            headers(other.getHeaders());
            params(other.getParams());
            auth(other.getAuth());
            formatterConfig(other.getFormatterConfig());
            return this;
        }

        /**
         * <p>Human-readable label for this webhook.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public UrlStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>Destination URL that receives the payload. Must use HTTPS. IP addresses are not accepted.</p>
         * <p>Type-specific URL requirements:</p>
         * <ul>
         * <li><code>slack</code>: Must start with <code>https://hooks.slack.com/</code>.</li>
         * <li><code>teams</code>: Hostname must match <code>*.webhook.office.com</code> or <code>*.webhook.office365.com</code>.</li>
         * <li><code>generic</code>: Any valid HTTPS domain.</li>
         * <li><code>custom</code>: Any valid HTTPS domain.</li>
         * </ul>
         * <p>When <code>type</code> is omitted, it is auto-detected from the URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("url")
        public _FinalStage url(@NotNull String url) {
            this.url = Objects.requireNonNull(url, "url must not be null");
            return this;
        }

        /**
         * <p>Custom payload formatter. Required when <code>type</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage formatterConfig(Nullable<FormatterConfigDto> formatterConfig) {
            if (formatterConfig.isNull()) {
                this.formatterConfig = null;
            } else if (formatterConfig.isEmpty()) {
                this.formatterConfig = Optional.empty();
            } else {
                this.formatterConfig = Optional.of(formatterConfig.get());
            }
            return this;
        }

        /**
         * <p>Custom payload formatter. Required when <code>type</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage formatterConfig(FormatterConfigDto formatterConfig) {
            this.formatterConfig = Optional.ofNullable(formatterConfig);
            return this;
        }

        /**
         * <p>Custom payload formatter. Required when <code>type</code> is <code>custom</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "formatter_config", nulls = Nulls.SKIP)
        public _FinalStage formatterConfig(Optional<FormatterConfigDto> formatterConfig) {
            this.formatterConfig = formatterConfig;
            return this;
        }

        /**
         * <p>Authentication forwarded with each delivery. Supported types:</p>
         * <ul>
         * <li><code>bearer</code>: Adds an <code>Authorization: Bearer &lt;token&gt;</code> header.</li>
         * <li><code>api_key</code>: Adds a custom header with the specified name and value.</li>
         * <li><code>basic</code>: Adds an <code>Authorization: Basic &lt;credentials&gt;</code> header.</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage auth(CreateWebhookRequestDtoAuth auth) {
            this.auth = Optional.ofNullable(auth);
            return this;
        }

        /**
         * <p>Authentication forwarded with each delivery. Supported types:</p>
         * <ul>
         * <li><code>bearer</code>: Adds an <code>Authorization: Bearer &lt;token&gt;</code> header.</li>
         * <li><code>api_key</code>: Adds a custom header with the specified name and value.</li>
         * <li><code>basic</code>: Adds an <code>Authorization: Basic &lt;credentials&gt;</code> header.</li>
         * </ul>
         */
        @java.lang.Override
        @JsonSetter(value = "auth", nulls = Nulls.SKIP)
        public _FinalStage auth(Optional<CreateWebhookRequestDtoAuth> auth) {
            this.auth = auth;
            return this;
        }

        /**
         * <p>Query parameters appended to the webhook URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage params(Map<String, String> params) {
            this.params = Optional.ofNullable(params);
            return this;
        }

        /**
         * <p>Query parameters appended to the webhook URL.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "params", nulls = Nulls.SKIP)
        public _FinalStage params(Optional<Map<String, String>> params) {
            this.params = params;
            return this;
        }

        /**
         * <p>Custom HTTP headers forwarded with each delivery.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage headers(Map<String, String> headers) {
            this.headers = Optional.ofNullable(headers);
            return this;
        }

        /**
         * <p>Custom HTTP headers forwarded with each delivery.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "headers", nulls = Nulls.SKIP)
        public _FinalStage headers(Optional<Map<String, String>> headers) {
            this.headers = headers;
            return this;
        }

        @java.lang.Override
        public _FinalStage method(HttpMethod method) {
            this.method = Optional.ofNullable(method);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "method", nulls = Nulls.SKIP)
        public _FinalStage method(Optional<HttpMethod> method) {
            this.method = method;
            return this;
        }

        @java.lang.Override
        public _FinalStage deliveryMode(DeliveryMode deliveryMode) {
            this.deliveryMode = Optional.ofNullable(deliveryMode);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "delivery_mode", nulls = Nulls.SKIP)
        public _FinalStage deliveryMode(Optional<DeliveryMode> deliveryMode) {
            this.deliveryMode = deliveryMode;
            return this;
        }

        @java.lang.Override
        public _FinalStage type(WebhookType type) {
            this.type = Optional.ofNullable(type);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "type", nulls = Nulls.SKIP)
        public _FinalStage type(Optional<WebhookType> type) {
            this.type = type;
            return this;
        }

        @java.lang.Override
        public CreateWebhookRequestDto build() {
            return new CreateWebhookRequestDto(
                    name,
                    url,
                    type,
                    deliveryMode,
                    method,
                    headers,
                    params,
                    auth,
                    formatterConfig,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
