/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.resources.webhooks.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import com.newscatcher.catchall.types.ApiKeyAuthDto;
import com.newscatcher.catchall.types.BasicAuthDto;
import com.newscatcher.catchall.types.BearerAuthDto;
import java.util.Objects;
import java.util.Optional;

public final class UpdateWebhookRequestDtoAuth {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private UpdateWebhookRequestDtoAuth(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static UpdateWebhookRequestDtoAuth bearer(BearerAuthDto value) {
        return new UpdateWebhookRequestDtoAuth(new BearerValue(value));
    }

    public static UpdateWebhookRequestDtoAuth apiKey(ApiKeyAuthDto value) {
        return new UpdateWebhookRequestDtoAuth(new ApiKeyValue(value));
    }

    public static UpdateWebhookRequestDtoAuth basic(BasicAuthDto value) {
        return new UpdateWebhookRequestDtoAuth(new BasicValue(value));
    }

    public boolean isBearer() {
        return value instanceof BearerValue;
    }

    public boolean isApiKey() {
        return value instanceof ApiKeyValue;
    }

    public boolean isBasic() {
        return value instanceof BasicValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<BearerAuthDto> getBearer() {
        if (isBearer()) {
            return Optional.of(((BearerValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ApiKeyAuthDto> getApiKey() {
        if (isApiKey()) {
            return Optional.of(((ApiKeyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BasicAuthDto> getBasic() {
        if (isBasic()) {
            return Optional.of(((BasicValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UpdateWebhookRequestDtoAuth
                && value.equals(((UpdateWebhookRequestDtoAuth) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitBearer(BearerAuthDto bearer);

        T visitApiKey(ApiKeyAuthDto apiKey);

        T visitBasic(BasicAuthDto basic);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(BearerValue.class),
        @JsonSubTypes.Type(ApiKeyValue.class),
        @JsonSubTypes.Type(BasicValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("bearer")
    @JsonIgnoreProperties("type")
    private static final class BearerValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private BearerAuthDto value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BearerValue() {}

        private BearerValue(BearerAuthDto value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBearer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BearerValue && equalTo((BearerValue) other);
        }

        private boolean equalTo(BearerValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UpdateWebhookRequestDtoAuth{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("api_key")
    @JsonIgnoreProperties("type")
    private static final class ApiKeyValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private ApiKeyAuthDto value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ApiKeyValue() {}

        private ApiKeyValue(ApiKeyAuthDto value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitApiKey(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ApiKeyValue && equalTo((ApiKeyValue) other);
        }

        private boolean equalTo(ApiKeyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UpdateWebhookRequestDtoAuth{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("basic")
    @JsonIgnoreProperties("type")
    private static final class BasicValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private BasicAuthDto value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BasicValue() {}

        private BasicValue(BasicAuthDto value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBasic(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BasicValue && equalTo((BasicValue) other);
        }

        private boolean equalTo(BasicValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UpdateWebhookRequestDtoAuth{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UpdateWebhookRequestDtoAuth{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
