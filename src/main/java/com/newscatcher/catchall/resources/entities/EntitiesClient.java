/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.newscatcher.catchall.resources.entities;

import com.newscatcher.catchall.core.ClientOptions;
import com.newscatcher.catchall.core.RequestOptions;
import com.newscatcher.catchall.resources.entities.requests.CreateEntitiesBatchRequest;
import com.newscatcher.catchall.resources.entities.requests.DeleteEntityRequest;
import com.newscatcher.catchall.resources.entities.requests.GetEntityRequest;
import com.newscatcher.catchall.resources.entities.requests.ListEntitiesRequest;
import com.newscatcher.catchall.resources.entities.requests.UpdateEntityRequest;
import com.newscatcher.catchall.types.CreateEntitiesBatchResponse;
import com.newscatcher.catchall.types.CreateEntityRequest;
import com.newscatcher.catchall.types.CreateEntityResponse;
import com.newscatcher.catchall.types.EntityListResponse;
import com.newscatcher.catchall.types.EntityResponse;

public class EntitiesClient {
    protected final ClientOptions clientOptions;

    private final RawEntitiesClient rawClient;

    public EntitiesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawEntitiesClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawEntitiesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a paginated list of entities belonging to the authenticated
     * organization. Supports filtering by status and entity type, and
     * sorting by name, status, or creation date.
     */
    public EntityListResponse listEntities() {
        return this.rawClient.listEntities().body();
    }

    /**
     * Returns a paginated list of entities belonging to the authenticated
     * organization. Supports filtering by status and entity type, and
     * sorting by name, status, or creation date.
     */
    public EntityListResponse listEntities(RequestOptions requestOptions) {
        return this.rawClient.listEntities(requestOptions).body();
    }

    /**
     * Returns a paginated list of entities belonging to the authenticated
     * organization. Supports filtering by status and entity type, and
     * sorting by name, status, or creation date.
     */
    public EntityListResponse listEntities(ListEntitiesRequest request) {
        return this.rawClient.listEntities(request).body();
    }

    /**
     * Returns a paginated list of entities belonging to the authenticated
     * organization. Supports filtering by status and entity type, and
     * sorting by name, status, or creation date.
     */
    public EntityListResponse listEntities(ListEntitiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.listEntities(request, requestOptions).body();
    }

    /**
     * Creates a new company entity and begins background enrichment.
     * <p>The entity status starts as <code>pending</code> and transitions to <code>ready</code> once
     * enrichment completes. Provide as much identifying information as
     * possible — <code>domain</code> is the highest-signal field because it is
     * unambiguous.</p>
     */
    public CreateEntityResponse createEntity(CreateEntityRequest request) {
        return this.rawClient.createEntity(request).body();
    }

    /**
     * Creates a new company entity and begins background enrichment.
     * <p>The entity status starts as <code>pending</code> and transitions to <code>ready</code> once
     * enrichment completes. Provide as much identifying information as
     * possible — <code>domain</code> is the highest-signal field because it is
     * unambiguous.</p>
     */
    public CreateEntityResponse createEntity(CreateEntityRequest request, RequestOptions requestOptions) {
        return this.rawClient.createEntity(request, requestOptions).body();
    }

    /**
     * Creates multiple entities in a single request. Each entity is
     * processed independently — a failure in one does not affect others.
     * <p>Returns an array of <code>{id, status}</code> objects in the same order as
     * the input array.</p>
     */
    public CreateEntitiesBatchResponse createEntitiesBatch(CreateEntitiesBatchRequest request) {
        return this.rawClient.createEntitiesBatch(request).body();
    }

    /**
     * Creates multiple entities in a single request. Each entity is
     * processed independently — a failure in one does not affect others.
     * <p>Returns an array of <code>{id, status}</code> objects in the same order as
     * the input array.</p>
     */
    public CreateEntitiesBatchResponse createEntitiesBatch(
            CreateEntitiesBatchRequest request, RequestOptions requestOptions) {
        return this.rawClient.createEntitiesBatch(request, requestOptions).body();
    }

    /**
     * Returns a single entity by ID with all attributes and current status.
     */
    public EntityResponse getEntity(String entityId) {
        return this.rawClient.getEntity(entityId).body();
    }

    /**
     * Returns a single entity by ID with all attributes and current status.
     */
    public EntityResponse getEntity(String entityId, RequestOptions requestOptions) {
        return this.rawClient.getEntity(entityId, requestOptions).body();
    }

    /**
     * Returns a single entity by ID with all attributes and current status.
     */
    public EntityResponse getEntity(String entityId, GetEntityRequest request) {
        return this.rawClient.getEntity(entityId, request).body();
    }

    /**
     * Returns a single entity by ID with all attributes and current status.
     */
    public EntityResponse getEntity(String entityId, GetEntityRequest request, RequestOptions requestOptions) {
        return this.rawClient.getEntity(entityId, request, requestOptions).body();
    }

    /**
     * Permanently deletes an entity. The entity is removed from all
     * datasets and the search index. This operation cannot be undone.
     */
    public void deleteEntity(String entityId) {
        this.rawClient.deleteEntity(entityId).body();
    }

    /**
     * Permanently deletes an entity. The entity is removed from all
     * datasets and the search index. This operation cannot be undone.
     */
    public void deleteEntity(String entityId, RequestOptions requestOptions) {
        this.rawClient.deleteEntity(entityId, requestOptions).body();
    }

    /**
     * Permanently deletes an entity. The entity is removed from all
     * datasets and the search index. This operation cannot be undone.
     */
    public void deleteEntity(String entityId, DeleteEntityRequest request) {
        this.rawClient.deleteEntity(entityId, request).body();
    }

    /**
     * Permanently deletes an entity. The entity is removed from all
     * datasets and the search index. This operation cannot be undone.
     */
    public void deleteEntity(String entityId, DeleteEntityRequest request, RequestOptions requestOptions) {
        this.rawClient.deleteEntity(entityId, request, requestOptions).body();
    }

    /**
     * Updates one or more fields of an existing entity.
     */
    public EntityResponse updateEntity(String entityId) {
        return this.rawClient.updateEntity(entityId).body();
    }

    /**
     * Updates one or more fields of an existing entity.
     */
    public EntityResponse updateEntity(String entityId, RequestOptions requestOptions) {
        return this.rawClient.updateEntity(entityId, requestOptions).body();
    }

    /**
     * Updates one or more fields of an existing entity.
     */
    public EntityResponse updateEntity(String entityId, UpdateEntityRequest request) {
        return this.rawClient.updateEntity(entityId, request).body();
    }

    /**
     * Updates one or more fields of an existing entity.
     */
    public EntityResponse updateEntity(String entityId, UpdateEntityRequest request, RequestOptions requestOptions) {
        return this.rawClient.updateEntity(entityId, request, requestOptions).body();
    }
}
